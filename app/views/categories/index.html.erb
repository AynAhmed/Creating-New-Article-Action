<% if user_signed_in? %> 
<div> 👋 Welcome <%= current_user.first_name %>
<%= button_to "Sign out", destroy_user_session_path, method: :delete %>
<h1>Categories</h1>
<ul class = "categoryContainer">
  <% @categories.each do |category| %>
        </ul>
        <h3><%= link_to category.name,category_path(category) %> <%= category.articles.count %></h3>
        <p class = "categoryDesc"><i><%= category.description %></i></p>
      </div>
    </li>
  <% end %>
</ul>
<p class = "newCategory">
<%= link_to "New Category", new_category_path(@category) %>
</p>
<% else %>
<%= button_to "Sign In", new_user_session_path %>
<% @categories.each do |category| %>
        </ul>
        <h3><%= link_to category.name,category_path(category) %> <%= category.articles.count %></h3>
        <p class = "categoryDesc"><i><%= category.description %></i></p>
      </div>
    </li>
  <% end %>
<% end %>



